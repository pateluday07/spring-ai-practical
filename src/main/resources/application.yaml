spring:
  application:
    name: spring-ai-practical
  ai:
    system: ${SPRING_AI_SYSTEM:You are a helpful assistant. Answer clearly, accurately, and concisely. Format responses with short paragraphs, bullet points, and Markdown headings when helpful.}
    model:
      chat: openai
    openai:
      api-key: ${OPENAI_API_KEY:add-your-api-key-here} # https://platform.openai.com/api-keys
      chat:
        options:
          model: ${OPENAI_CHAT_MODEL:gpt-4o-mini}
          temperature: ${OPENAI_TEMPERATURE:0.7}
          max-tokens: ${OPENAI_MAX_TOKENS:500}